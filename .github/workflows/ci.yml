# Guardrails: block merge if lint, type-check, unit tests, or E2E fail.
# Runs on every Pull Request to main. All jobs must pass to merge.
# Required secrets: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
# For E2E: use a SEPARATE Supabase project (dev/staging), NOT production. See docs/E2E-TEST-APPROACH.md.
# Optional: SUPABASE_PROD_PROJECT_REF = prod project ref (e.g. subdomain of prod URL). When set, E2E aborts if pointing at prod.
# Uses pull_request_target so secrets (Supabase for E2E) are available to same-repo PRs.
# Mitigation: we never run PR workflow code in a privileged context — every job checks out
# ref: head.sha and runs the PR branch code; the workflow YAML is from the base branch.
# See https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions#approving-workflows-from-forks
#
# --- Trigger strategy (Actions minutes) ---
# Runs ONLY on: pull_request (targeting main). Does NOT run on push to other branches or on every push.
# This keeps CI for merge safety while avoiding runs on feature-branch pushes (saves org minutes).
name: CI

on:
  pull_request_target: # required for secrets on PRs; mitigated by checkout ref: head.sha in every job
    branches: [main]

# Allow artifact upload and PR/check writes (fixes "Resource not accessible by integration" on pull_request)
permissions:
  contents: read
  actions: write
  checks: write
  pull-requests: write

jobs:
  # Security: dependency vulns (fail on high/critical) + code/secret audit (fail on dangerous patterns only).
  # Only run on same-repo PRs so fork PRs never receive secrets (pull_request_target mitigation).
  security:
    name: security
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request_target' || github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v2
        with:
          version: 8.15.4
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install
      - name: Dependency audit
        run: |
          for i in 1 2 3; do
            pnpm audit --audit-level=high 2>&1 | tee /tmp/audit.log
            exit_code=${PIPESTATUS[0]}
            if [ "$exit_code" -eq 0 ]; then
              exit 0
            fi
            if grep -qE '500|Internal Server Error|ERR_PNPM_AUDIT_BAD_RESPONSE' /tmp/audit.log; then
              echo "::warning::Audit failed due to registry error (attempt $i/3). Retrying in 30s..."
              if [ "$i" -lt 3 ]; then
                sleep 30
              else
                echo "::warning::Skipping audit after 3 attempts due to registry errors."
                exit 0
              fi
            else
              exit $exit_code
            fi
          done

  # Aikido (dependency/SAST/IaC) runs via the Aikido PR checks app; do not add the
  # AikidoSec/github-actions-workflow action or you get 422 "repository already protected".

  code-review:
    name: code-review
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request_target' || github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v2
        with:
          version: 8.15.4
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install
      - name: Automated code review
        run: pnpm run code-review

  lint:
    name: lint
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request_target' || github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v2
        with:
          version: 8.15.4
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install
      - run: pnpm lint

  type-check:
    name: type-check
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request_target' || github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v2
        with:
          version: 8.15.4
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install
      - run: pnpm type-check

  test-unit:
    name: Unit tests (Vitest)
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request_target' || github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v2
        with:
          version: 8.15.4
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install
      - name: Run unit tests
        working-directory: apps/web
        run: pnpm test:unit

  test-pwyl-api:
    name: PWYL API and email tests
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request_target' || github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v2
        with:
          version: 8.15.4
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install
      - name: Run API tests
        working-directory: apps/web
        run: pnpm test:api
      - name: Run email tests
        working-directory: apps/web
        run: pnpm test:emails

  test-localhost:
    name: E2E on localhost
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request_target' || github.event.pull_request.head.repo.full_name == github.repository
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v2
        with:
          version: 8.15.4
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install
      - name: Cache Playwright browsers
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-chromium-${{ hashFiles('pnpm-lock.yaml', 'apps/web/package.json') }}
      - name: Install Playwright Browsers
        working-directory: apps/web
        run: |
          if [ "${{ steps.playwright-cache.outputs.cache-hit }}" != 'true' ]; then
            pnpm exec playwright install --with-deps chromium
          else
            pnpm exec playwright install-deps chromium
          fi
      - name: Run Playwright tests
        working-directory: apps/web
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_PROD_PROJECT_REF: ${{ secrets.SUPABASE_PROD_PROJECT_REF }}
          PLAYWRIGHT_TEST_BASE_URL: http://localhost:3000
        run: pnpm test:e2e
      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: playwright-report-localhost
          path: apps/web/playwright-report/
          retention-days: 7

  # Optional: PWYL E2E (sandbox). Allow failure until stable. Requires secrets:
  # POLAR_PWYL_BASE_PRODUCT_ID_SANDBOX, POLAR_ACCESS_TOKEN_SANDBOX, POLAR_WEBHOOK_SECRET_SANDBOX
  test-pwyl-e2e:
    name: PWYL E2E (sandbox)
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request_target' || github.event.pull_request.head.repo.full_name == github.repository
    continue-on-error: true
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v2
        with:
          version: 8.15.4
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install
      - name: Cache Playwright browsers
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-chromium-${{ hashFiles('pnpm-lock.yaml', 'apps/web/package.json') }}
      - name: Install Playwright Browsers
        working-directory: apps/web
        run: pnpm exec playwright install --with-deps chromium
      - name: Run PWYL E2E
        working-directory: apps/web
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_PROD_PROJECT_REF: ${{ secrets.SUPABASE_PROD_PROJECT_REF }}
          NEXT_PUBLIC_PRICING_ENABLED: 'true'
          NEXT_PUBLIC_PWYL_PRICING_ENABLED: 'true'
          POLAR_PWYL_BASE_PRODUCT_ID: ${{ secrets.POLAR_PWYL_BASE_PRODUCT_ID_SANDBOX }}
          POLAR_ACCESS_TOKEN: ${{ secrets.POLAR_ACCESS_TOKEN_SANDBOX }}
          POLAR_SUCCESS_URL: ${{ secrets.PLAYWRIGHT_TEST_BASE_URL || 'http://localhost:3000' }}/dashboard/settings?tab=subscription
        run: pnpm test:e2e:pwyl
