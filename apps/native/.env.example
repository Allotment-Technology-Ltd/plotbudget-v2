# =============================================================================
# DEV / PRE-PRODUCTION (local development and Expo Go)
# =============================================================================
# Copy this file to .env and set values from your PRE-PRODUCTION Supabase project
# (same project as web app's .env.local). Used when you run: pnpm start, pnpm android.
#
# Get values from: Supabase Dashboard → Project Settings → API
#   - Project URL        → EXPO_PUBLIC_SUPABASE_URL
#   - anon public key    → EXPO_PUBLIC_SUPABASE_ANON_KEY
#
# Only these two are needed in the app. Do not add the service_role key.
# =============================================================================
EXPO_PUBLIC_SUPABASE_URL=https://your-preprod-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_preprod_anon_key

# =============================================================================
# PRODUCTION
# =============================================================================
# For production builds (e.g. Play Store), do NOT put prod keys in .env.
# Set EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_ANON_KEY in EAS:
#   eas secret:create --name EXPO_PUBLIC_SUPABASE_URL --value "https://your-prod-project.supabase.co" --scope project --type string
#   eas secret:create --name EXPO_PUBLIC_SUPABASE_ANON_KEY --value "your_prod_anon_key" --scope project --type string
# Use the production build profile (e.g. eas build -p android --profile production)
# so the built app uses the prod Supabase project (same as web production).
# =============================================================================
