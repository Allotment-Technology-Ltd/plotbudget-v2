# =============================================================================
# PRODUCTION DEFAULTS (.env)
# =============================================================================
# Copy to .env for production. Loaded first; .env.local (if present) overrides.
# For pre-prod: copy .env.local.example to .env.local — no need to change .env.
# =============================================================================
EXPO_PUBLIC_APP_URL=https://app.plotbudget.com

# Production Supabase (or set in EAS secrets for cloud builds)
# EXPO_PUBLIC_SUPABASE_URL=https://your-prod-project.supabase.co
# EXPO_PUBLIC_SUPABASE_ANON_KEY=your_prod_anon_key

# PostHog (optional)
# EXPO_PUBLIC_POSTHOG_KEY=
# EXPO_PUBLIC_POSTHOG_HOST=https://eu.posthog.com

# =============================================================================
# DEV / PRE-PRODUCTION (local development and Expo Go)
# =============================================================================
# Copy .env.local.example to .env.local and set values from your PRE-PRODUCTION
# Supabase project (same project as web app's .env.local). Used when you run:
#   pnpm start, pnpm android.
# Get values from: Supabase Dashboard → Project Settings → API
# =============================================================================

# =============================================================================
# EAS production builds (Play Store)
# =============================================================================
# Do NOT put prod keys in .env. Set in EAS:
#   eas secret:create --name EXPO_PUBLIC_SUPABASE_URL --value "..." --scope project --type string
#   eas secret:create --name EXPO_PUBLIC_SUPABASE_ANON_KEY --value "..." --scope project --type string
# Use production build profile so the app uses the prod Supabase project.
# =============================================================================
