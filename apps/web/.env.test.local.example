# E2E / Playwright: copy to .env.test.local.
# Loaded after .env.local (see playwright.config.ts). Use .env.local for shared vars; this file for test overrides only.

# Playwright
PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000

# Supabase: same as .env.local (dev project). Ensure .env.local has NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY.
# Optional: override here if you use a different test project.
