# Dependencies
node_modules/
.pnpm-store/
package-lock.json
.yarn/cache/
.yarn/unplugged/
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Build outputs
.next/
dist/
build/
out/

# Turbo
.turbo/

# Environment (never commit secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# IDE & editors
.vscode/
.idea/
*.swp
*.swo
*.sublime-workspace
*.sublime-project

# OS
.DS_Store
Thumbs.db
*.lnk

# Logs & debug
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/
playwright-report/
test-results/
playwright/.cache/
**/.auth/

# Linting & formatting
.eslintcache

# TypeScript
*.tsbuildinfo

# Supabase CLI
supabase/.temp/

# Vite (marketing app cache)
.vite/

# Vercel & deployment
.vercel/

# Expo (native app build/cache)
.expo/

# Misc
.cache/
codebase-audit.md

# Local Python venvs (e.g. for scripts/crop-showcase-to-border.py)
.venv/
.venv-crop/

# Cursor IDE (local plans, rules, skills – do not track)
.cursor/

# Allow sanity-studio lockfile for reproducible CI (npm ci)
!sanity-studio/package-lock.json
# Secrets that should never be committed (belt-and-suspenders)
.env.*.key
*.pem
*.key
secrets.json
credentials.json