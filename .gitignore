# Dependencies
node_modules/
.pnpm-store/
package-lock.json
.yarn/cache/
.yarn/unplugged/
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Build outputs
.next/
dist/
build/
out/

# Turbo
.turbo/

# Environment (never commit secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
.env.example
.env*.example

# IDE & editors
.vscode/
.idea/
*.swp
*.swo
*.sublime-workspace
*.sublime-project

# OS
.DS_Store
Thumbs.db
*.lnk

# Logs & debug
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/
playwright-report/
test-results/
playwright/.cache/
**/.auth/

# Linting & formatting
.eslintcache

# TypeScript
*.tsbuildinfo

# Supabase CLI
supabase/.temp/

# Vite (marketing app cache)
.vite/

# Vercel & deployment
.vercel/

# Expo (native app build/cache)
.expo/

# Misc
.cache/
codebase-audit.md

# Local Python venvs (e.g. for scripts/crop-showcase-to-border.py)
.venv/
.venv-crop/

# Cursor IDE (local plans, rules, skills – do not track)
# we still keep most of the directory ignored, but explicitly allow the
# two subfolders we care about so they can be checked in.
.cursor/*
!.cursor/skills/
!.cursor/skills/**
!.cursor/rules/
!.cursor/rules/**

# but we want to version‑control the shared rules and skills used by
# remote helpers.  unignore those subdirectories so they can be
# committed and kept in sync with `rules.yaml`.

# Allow sanity-studio lockfile for reproducible CI (npm ci)
!sanity-studio/package-lock.json
# Secrets that should never be committed (belt-and-suspenders)
.env.*.key
*.pem
*.key
secrets.json
credentials.json