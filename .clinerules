# (deprecated)
This file was the old CLI rules configuration.  The canonical,
version-controlled rules are now stored in `rules.yaml` at the
repository root.  Please update any references to point at that file.

The original content has been moved; keep this stub for backward
compatibility or delete it once tooling no longer looks for it.
